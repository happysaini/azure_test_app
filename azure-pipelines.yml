# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'macOS-10.14'

steps:
- bash: | 
    python --version
    echo $BuildID
- task: SendEmail@1
  inputs:
    To: 'hsaini@qasource.com'
    From: 'hsaini@qasource.com'
    Subject: 'Test'
    Body: 'Will update it'
    BodyAsHtml: false
    AddAttachment: false