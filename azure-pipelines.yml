# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

parameters:
- name: EMAIL_PASS
  type: string
  default: password

pool:
  vmImage: 'macOS-10.14'

steps:
- bash: | 
    echo ${{ parameters.EMAIL_PASS }}